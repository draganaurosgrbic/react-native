import { NavigationStackOptions } from "react-navigation-stack";
import { COLORS } from "../constants/colors";

export const STACK_CONFIG: NavigationStackOptions = {
  headerStyle: {
    backgroundColor: COLORS.primary,
  },
  headerTintColor: "white",
};
